# Validated exact pins (project standard is == not >=). Each at or above its
# historical floor; current stable releases verified against PyPI.
xlsxwriter==3.2.9
pandas==3.0.3
pyyaml==6.0.3
matplotlib==3.10.9
