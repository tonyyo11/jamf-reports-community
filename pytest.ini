[pytest]
testpaths = tests
markers =
    integration: end-to-end or file-backed tests using committed fixtures.
    local: tests that rely on a larger local-only workspace.
    live: tests that require a live jamf-cli binary or authenticated Jamf tenant.
